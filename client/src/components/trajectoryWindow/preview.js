import React from "react";
import { H5, Card } from "@blueprintjs/core";

export default class Preview extends React.PureComponent {
  render() {
    return (
      <Card>
        <H5>Trajectory Preview</H5>
        {/* TODO: trajectory preview by cytoscape*/}
      </Card>
    );
  }
}
