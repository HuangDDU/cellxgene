import React from "react";
import { H5, Card } from "@blueprintjs/core";

export default class Benchmark extends React.PureComponent {
  render() {
    return (
      <Card>
        <H5>Trajectory Benchmark</H5>
      </Card>
    );
  }
}
